import React, { useState, FormEvent } from 'react';
import { useNavigate } from 'react-router-dom';
import { ArrowLeft, Map } from 'lucide-react';
import { Card } from '../components/ui/Card';
import { Button } from '../components/ui/Button';
import { Input } from '../components/ui/Input';
import { Select } from '../components/ui/Select';
import { Textarea } from '../components/ui/Textarea';
import { motion } from 'motion/react';

export function AddFishingGround() {
  const navigate = useNavigate();
  const [loading, setLoading] = useState(false);
  const [formData, setFormData] = useState({
    name: '',
    type: 'lake' as const,
    area: '',
    maxDepth: '',
    description: ''
  });

  const handleChange = (e: React.ChangeEvent<HTMLInputElement | HTMLSelectElement | HTMLTextAreaElement>) => {
    setFormData(prev => ({ ...prev, [e.target.name]: e.target.value }));
  };

  const handleSubmit = async (e: FormEvent) => {
    e.preventDefault();
    setLoading(true);

    // Mock save - in production, this would call API
    await new Promise(resolve => setTimeout(resolve, 1000));

    setLoading(false);
    navigate('/lowiska');
  };

  const typeOptions = [
    { value: 'lake', label: 'Jezioro' },
    { value: 'river', label: 'Rzeka' },
    { value: 'pond', label: 'Staw' },
    { value: 'reservoir', label: 'Zalew' },
    { value: 'canal', label: 'Kanał' }
  ];

  return (
    <div className="max-w-3xl mx-auto space-y-6">
      {/* Header */}
      <motion.div
        initial={{ opacity: 0, y: -20 }}
        animate={{ opacity: 1, y: 0 }}
      >
        <Button
          variant="outline"
          onClick={() => navigate('/lowiska')}
          className="mb-4 flex items-center gap-2"
        >
          <ArrowLeft className="w-4 h-4" />
          Wróć do listy
        </Button>

        <h1 className="text-3xl font-bold text-gray-900 mb-2">Dodaj łowisko</h1>
        <p className="text-gray-600">Wprowadź informacje o nowym łowisku</p>
      </motion.div>

      {/* Form */}
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ delay: 0.1 }}
      >
        <Card className="p-8">
          <form onSubmit={handleSubmit} className="space-y-6">
            <Input
              label="Nazwa łowiska"
              name="name"
              placeholder="np. Jezioro śniardwy"
              value={formData.name}
              onChange={handleChange}
              required
            />

            <Select
              label="Typ Łowiska"
              name="type"
              options={typeOptions}
              value={formData.type}
              onChange={handleChange}
              required
            />

            <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
              <Input
                label="Powierzchnia (ha)"
                name="area"
                type="number"
                step="0.1"
                placeholder="np. 113.8"
                value={formData.area}
                onChange={handleChange}
                required
              />

              <Input
                label="Maksymalna głębokość (m)"
                name="maxDepth"
                type="number"
                step="0.1"
                placeholder="np. 23"
                value={formData.maxDepth}
                onChange={handleChange}
                required
              />
            </div>

            <Textarea
              label="Opis"
              name="description"
              placeholder="Opisz łowisko, gatunki ryb, dostępność..."
              value={formData.description}
              onChange={handleChange}
              rows={4}
              required
            />

            {/* Map Placeholder */}
            <div>
              <label className="block text-sm font-medium text-gray-700 mb-2">
                Zaznacz granice na mapie
              </label>
              <div className="relative h-64 bg-gradient-to-br from-blue-100 to-green-100 rounded-lg border-2 border-dashed border-gray-300 flex items-center justify-center">
                <div className="text-center">
                  <Map className="w-12 h-12 text-gray-400 mx-auto mb-3" />
                  <p className="text-sm font-medium text-gray-600 mb-1">
                    Mapa - przeciągnij aby zaznaczyć granice
                  </p>
                  <p className="text-xs text-gray-500">
                    (Funkcjonalność dostępna po podłączeniu Leaflet)
                  </p>
                </div>
              </div>
            </div>

            {/* Actions */}
            <div className="flex items-center justify-end gap-3 pt-4">
              <Button
                type="button"
                variant="secondary"
                onClick={() => navigate('/lowiska')}
              >
                Anuluj
              </Button>
              <Button
                type="submit"
                disabled={loading}
              >
                {loading ? 'Dodawanie...' : 'Dodaj łowisko'}
              </Button>
            </div>
          </form>
        </Card>
      </motion.div>

      {/* Info Box */}
      <motion.div
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 0.2 }}
      >
        <Card className="p-4 bg-blue-50 border-blue-200">
          <p className="text-sm text-blue-800">
            ?? <strong>Wskazówka:</strong> Im dokładniejsze informacje podasz, tym łatwiej będzie Ci planować kolejne wyprawy wędkarskie.
          </p>
        </Card>
      </motion.div>
    </div>
  );
}
