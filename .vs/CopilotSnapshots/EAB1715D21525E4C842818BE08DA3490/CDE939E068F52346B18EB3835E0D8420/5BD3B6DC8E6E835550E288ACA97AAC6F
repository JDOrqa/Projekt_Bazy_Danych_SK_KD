import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    host: true,
    allowedHosts: ['unveritable-fumier-araceli.ngrok-free.dev'],
    watch: {
      // Wyłącz file watching w Docker aby oszczędzić pamięć
      usePolling: false,
      ignored: ['**/node_modules/**', '**/.git/**'],
    },
    hmr: {
      protocol: 'ws',
      host: 'localhost',
      port: 5173,
    }
  }
})