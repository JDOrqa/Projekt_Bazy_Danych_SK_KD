import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
	allowedHosts: ['unveritable-fumier-araceli.ngrok-free.dev'],
    host: true,
    proxy: {
      // Opcjonalne: przekierowanie zapytań API do backendu (unikamy CORS w dev)
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
    },
  },
})