from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select, update, func as sql_func, cast, String, literal_column
from sqlalchemy.sql import func
from sqlalchemy.orm import Query
from geoalchemy2 import WKTElement
from geoalchemy2.functions import ST_AsText
import models
import schemas

async def get_user_by_email(db: AsyncSession, email: str):
    result = await db.execute(select(models.Uzytkownik).where(models.Uzytkownik.email == email))
    return result.scalar_one_or_none()

async def create_user(db: AsyncSession, user: schemas.UserCreate):
    hashed = models.Uzytkownik.hash_password(user.password)
    db_user = models.Uzytkownik(
        email=user.email,
        haslo_hash=hashed,
        imie=user.imie,
        nazwisko=user.nazwisko,
        zgoda_marketingowa=user.zgoda_marketingowa
    )
    db.add(db_user)
    await db.commit()
    await db.refresh(db_user)
    return db_user

async def update_user(db: AsyncSession, user_id: int, update_data: dict):
    await db.execute(update(models.Uzytkownik).where(models.Uzytkownik.id == user_id).values(**update_data))
    await db.commit()
    result = await db.execute(select(models.Uzytkownik).where(models.Uzytkownik.id == user_id))
    return result.scalar_one_or_none()

async def create_lowisko(db: AsyncSession, lowisko: schemas.LowiskoCreate, wlasciciel_id: int):
    granice_geom = None
    if lowisko.granice:
        wkt_str = lowisko.granice
        if wkt_str.startswith('SRID=4326;'):
            wkt_str = wkt_str.replace('SRID=4326;', '')
        granice_geom = WKTElement(wkt_str, srid=4326)
    
    db_lowisko = models.Lowisko(
        nazwa=lowisko.nazwa,
        typ=lowisko.typ,
        granice=granice_geom,
        powierzchnia_ha=lowisko.powierzchnia_ha,
        glebokosc_max=lowisko.glebokosc_max,
        opis=lowisko.opis,
        wlasciciel_id=wlasciciel_id
    )
    db.add(db_lowisko)
    await db.commit()
    await db.refresh(db_lowisko)
    return db_lowisko

async def delete_user(db: AsyncSession, user_id: int):
    await db.execute(update(models.Uzytkownik).where(models.Uzytkownik.id == user_id).values(deleted_at=func.now()))
    await db.commit()

async def delete_lowisko(db: AsyncSession, lowisko_id: int):
    await db.execute(update(models.Lowisko).where(models.Lowisko.id == lowisko_id).values(deleted_at=func.now()))
    await db.commit()

async def get_lowiska(db: AsyncSession, skip=0, limit=100):
# Użyj ST_AsText() aby pobrać geometrię jako WKT string
query = select(
    models.Lowisko.id,
    models.Lowisko.nazwa,
    models.Lowisko.typ,
    ST_AsText(models.Lowisko.granice).label('granice'),
    models.Lowisko.powierzchnia_ha,
    models.Lowisko.glebokosc_max,
    models.Lowisko.opis,
    models.Lowisko.wlasciciel_id,
    models.Lowisko.created_at,
    models.Lowisko.updated_at,
    models.Lowisko.deleted_at,
).where(models.Lowisko.deleted_at.is_(None)).offset(skip).limit(limit)
    
result = await db.execute(query)
rows = result.all()
    
# Konwertuj rows do obiektów Lowisko
lowiska = []
for row in rows:
    lowisko = models.Lowisko()
    lowisko.id = row.id
    lowisko.nazwa = row.nazwa
    lowisko.typ = row.typ
    lowisko.granice = row.granice  # Już jest WKT string
    lowisko.powierzchnia_ha = row.powierzchnia_ha
    lowisko.glebokosc_max = row.glebokosc_max
    lowisko.opis = row.opis
    lowisko.wlasciciel_id = row.wlasciciel_id
    lowisko.created_at = row.created_at
    lowisko.updated_at = row.updated_at
    lowisko.deleted_at = row.deleted_at
    lowiska.append(lowisko)
    
return lowiska


async def get_lowisko_by_id(db: AsyncSession, lowisko_id: int):
# Użyj ST_AsText() aby pobrać geometrię jako WKT string
query = select(
    models.Lowisko.id,
    models.Lowisko.nazwa,
    models.Lowisko.typ,
    ST_AsText(models.Lowisko.granice).label('granice'),
    models.Lowisko.powierzchnia_ha,
    models.Lowisko.glebokosc_max,
    models.Lowisko.opis,
    models.Lowisko.wlasciciel_id,
    models.Lowisko.created_at,
    models.Lowisko.updated_at,
    models.Lowisko.deleted_at,
).where(models.Lowisko.id == lowisko_id).where(models.Lowisko.deleted_at.is_(None))
    
result = await db.execute(query)
row = result.first()
    
if not row:
    return None
    
# Konwertuj row do obiektu Lowisko
lowisko = models.Lowisko()
lowisko.id = row.id
lowisko.nazwa = row.nazwa
lowisko.typ = row.typ
lowisko.granice = row.granice  # Już jest WKT string
lowisko.powierzchnia_ha = row.powierzchnia_ha
lowisko.glebokosc_max = row.glebokosc_max
lowisko.opis = row.opis
lowisko.wlasciciel_id = row.wlasciciel_id
lowisko.created_at = row.created_at
lowisko.updated_at = row.updated_at
lowisko.deleted_at = row.deleted_at
    
return lowisko

async def update_lowisko(db: AsyncSession, lowisko_id: int, lowisko: schemas.LowiskoCreate):
    granice_geom = None
    if lowisko.granice:
        wkt_str = lowisko.granice
        if wkt_str.startswith('SRID=4326;'):
            wkt_str = wkt_str.replace('SRID=4326;', '')
        granice_geom = WKTElement(wkt_str, srid=4326)
    
    update_data = {
        'nazwa': lowisko.nazwa,
        'typ': lowisko.typ,
        'granice': granice_geom,
        'powierzchnia_ha': lowisko.powierzchnia_ha,
        'glebokosc_max': lowisko.glebokosc_max,
        'opis': lowisko.opis
    }
    
    await db.execute(update(models.Lowisko).where(models.Lowisko.id == lowisko_id).values(**update_data))
    await db.commit()
    result = await db.execute(select(models.Lowisko).where(models.Lowisko.id == lowisko_id))
    return result.scalar_one_or_none()